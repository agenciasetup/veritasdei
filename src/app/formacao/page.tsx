/**
 * `/formacao` é a rota canônica do hub de estudo (premium).
 * `/aprender` permanece como redirect 301 (ver next.config.ts).
 */
import AprenderPage from "../aprender/page"

export default AprenderPage
