/**
 * `/igrejas` é a rota canônica do hub de paróquias/igrejas.
 * `/paroquias` (e subrotas) permanece como redirect 301 durante a
 * transição (ver next.config.ts).
 */
import ParoquiasPage from "../paroquias/page"

export default ParoquiasPage
