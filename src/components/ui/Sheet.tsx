/**
 * Primitivo canônico de bottom sheet. Re-export do BottomSheet existente
 * em components/mobile — mantém o path canônico de primitivos em ui/.
 */
export { BottomSheet as Sheet, default } from "@/components/mobile/BottomSheet"
export type { BottomSheetProps as SheetProps } from "@/components/mobile/BottomSheet"
